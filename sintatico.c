#include "cabecalho.h"

int verifica_troca_sintatico(token t, linha* tabela){
/*	int tam_tabela = tamanho_tabela(tabela);
	int i=0;
	int valido;
	while(i < tam_tabela){

		if(EA == tabela[i].EA){
			valido = valida(c,tabela[i].trans);			

			if( valido ){
				acao(c,tabela[i].ACTION);
				EA = tabela[i].PROX;
				return 1;
			}
		}
		i++;
	}
	return 0;*/
}
/*
void automato_sintatico(linha *tabela,Lista *L){
	char c;
	int aux;
	Lista prox;
	valido=0;
	EA=0;
	int i=0;
	prox = L;
	
	while(prox){
		if (!( aux = verifica_troca_sintatico(c,tabela) )){
			printf("\nError input\n");
			exit(1);
		}
		prox = prox->prox;
	}

}

*/